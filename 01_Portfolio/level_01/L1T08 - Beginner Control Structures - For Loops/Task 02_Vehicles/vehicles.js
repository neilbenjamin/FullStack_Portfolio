let carsArray = ["Opel", "Toyota", "Ford", "BMW", "Merecedes"]; //Array conytaining the car strings in indexes 0 - 4.

for (let i = 0; i < carsArray.length; i++) {
  console.log(`I would like to drive a ` + carsArray[i]);// calling array indexes from 0 to the end of the array length with each new iteration. 
}
